一）稀疏数组与队列
    1）稀疏数组 SparseArray
        当一个数组中大部分元素为相同值时，可以通过稀疏数组保存该数组
        * 把具有不同值的元素的行列及值记录在一个小规模的数组中从而缩小程序的规模
    2）二维数组转换稀疏数组的思路
        1.遍历原始的二维数组，得到有效数据的个数sum
        2.根据sum可以创建叙述数组sparseArray
        3.讲二维数组的有效数据存储到稀疏数组
    3）稀疏数组转换二维数组的思路
        1.先读取稀疏数组的第一行，根据第一行的数据，创建原始二维数据
        2.根据读取稀疏数组后几行的数据，并赋值给原始的二维数组即可
